---
infra_helm_application:
  name: "helm"
  type: "binary"
  version: "{{ 'latest_' + ansible_architecture }}"
  installed: true

infra_helm_paths:
  root: "/opt/helm"

infra_helm_owners:
  root:
    user: "helm"
    group: "helm"
...
